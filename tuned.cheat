% tuned
# Show active tuned profile
tuned-adm active

# List available tuned profiles
tuned-adm list

# Change tuned profile
tuned-adm profile <PROFILE>
$ PROFILE: tuned-adm list | awk '$1 ~ /^-/' --- --column 2 --delimiter '\s'
